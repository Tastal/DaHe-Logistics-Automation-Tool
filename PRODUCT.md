# Product

## Register

product

## Users

主要用户是大禾煤业物流公司的财务操作人员。用户熟悉运单、磅单、装卸车、结算等业务，但不应被要求理解 OCR、GPU、DNS、浏览器自动化或数据库等技术概念。

开发者负责部署、模板维护、故障诊断和版本升级。生产环境与开发环境分别位于两台 Windows 电脑，不自动共享运行状态或数据库。

系统按中小企业的单机使用方式设计，不建设人员账号、姓名、工号、处理人、审核人或操作归属功能。人工动作只记录业务内容、原因、时间和证据版本；Windows 文件权限、本地会话和维护授权只用于无感安全保护，不进入业务记录或页面。

本项目只服务大禾自家公司。稳定和能用优先于技术完整度；在满足业务、安全、数据保留和故障恢复要求时，始终选择步骤更少、依赖更少、部署更简单、以后更容易看懂和修改的方案。不能因为某种架构或开源工具更流行就引入它，也不为只有一台公司电脑和一台开发电脑的规模预建复杂平台。

## Product Purpose

大禾物流系统用于把煤业物流中的重复核对、证据整理和数据沉淀工作变成稳定、可追溯的本地流程。

首期重点完成：

- 在统一的“运费结算”工作区核对装货、卸货磅单与成丰平台重量，并隔离问题运单。
- 装卸车明细提取、持续补齐和正式报表输出。
- 正常运单的结算前复核、筛选和人工交接。
- 问题运单、人工判断、历史数据和运行证据的长期结构化保存。

实际确认结算和支付永久由人员在成丰平台完成。派单模块首期只保留产品位置，不实施真实派单流程。

本地操作台的业务入口只保留“运费结算”“装卸车明细”“派单”。“历史数据”“系统设置”“版本更新”“退出程序”固定为左下角同一行的四个图标工具。“运单审核”是运费结算内部的机器阶段，不单独成为页面；待处理事项在所属业务页面内解决，不建立“今日工作”“待处理”或“人工复核”总入口。

成丰业务按签约主体分为“山西贵恩博”和“上海晋亿晟”。操作台在“业务”标题旁显示当前主体，切换只改变本地数据视图；只有人员在对应业务页点击“启动”时，程序才在唯一成丰标签中切换到该主体并读取。两个主体的任务、结果、人工核对、历史和装卸车报表相互隔离，不能用另一主体的数据补足空结果。纳入双主体前的既有历史统一归属“山西贵恩博”。

运费结算页面只展示最新一次获取产生的运单信息、磅单图片、识别净重、平台净重和业务判断。一次获取对应一个完整在线采集和一个离线审核 Job；业务人员不选择或启动内部审核任务。离线审核进行时，结果按照本次成丰列表的冻结顺序从上到下实时追加，页面只显示已经连续完成的前缀。人员可直接选择“确认无误”或“异常”，不在本地修改净重，不填写原因、正确值、备注或人员身份。问题运单只在本地隔离，程序不自动交换磅单，也不执行撤销收货、结算确认、支付或其他平台写操作。

运费结算的数量直接显示在“全部、待核对、问题运单、审核通过／可结算”筛选按钮中，不再建立重复的概况区域。桌面端优先让运单基本信息、装货磅单、卸货磅单和当前人工动作同时处于首屏；图片保持完整比例，并继续支持放大查看。

操作台中的按钮用途、禁用原因和非必要技术解释使用可由鼠标悬浮或键盘聚焦打开的提示。当前业务状态、错误、字段名、安全确认后果和完成工作所必需的步骤不能只藏在提示中。

系统默认进入运行诊断，只保留运行诊断、识别模板和参数设置。运行诊断提供可实时查看、复制和导出的脱敏后台日志；六个日志与诊断操作位于同一标题行，空间不足时收进“更多”，不再显示任务、本地资源、数据管理或“最近问题”。日志用于在其他电脑发生问题时协助诊断，不得记录凭据、维护码、绝对路径、原始 OCR 正文或子进程协议内容。诊断包只能由使用人员主动导出，包含最近操作摘要、环境快照和最近七天脱敏日志，不自动上传，不包含数据库、图片、浏览器资料、凭据、平台响应或 OCR 原文。

正式软件以当前 Windows 用户身份安装，程序文件与用户数据分开。桌面始终只有“大禾物流自动化平台”一个入口，稳定启动器根据已校验的当前版本指针启动业务程序。更新只从固定 GitHub 仓库的正式 Release 检查；检测到新版只提示，必须由使用人员确认安装，业务任务未结束时不允许更新。应用包支持断点续传，也允许人员导入已下载的正式清单和应用 ZIP；两种方式执行相同的版本、Schema、大小和哈希校验。断网、下载中断或更新失败不影响已安装版本继续使用，不增加公共代理或另一套在线发布源。

真实成丰数据只通过受控只读连接获取。DaHe 启动后异步维护一个使用专属 Profile 的可见 Edge 窗口；空闲时窗口归人员使用，程序不导航、不记录人工操作。业务人员在“运费结算”或“装卸车明细”点击“启动”，就是对该次只读 Job 的明确授权。启动后只复用一个成丰标签页并进入当前业务固定页面：运费结算使用 `/billablewaybill`，装卸车明细使用 `/wayBill`；最小化窗口恢复显示并选中该页。程序在同一页内安装本次只读请求能力，结束后清除能力、释放资源并保留页面。已有多个成丰标签时只保留一个，不关闭其他网站。需要登录时先尝试已保存凭据，仍失败时最多保留一个人工登录窗口。操作台刷新、关闭或重新打开不能触发重复登录、导航或控制交还。连接不得复用旧浏览器资料、旧程序数据或账号文件；程序不得执行结算确认、付款、撤销收货或其他平台写操作。

成丰账号密码只能由本地操作台写入 Windows Credential Manager 的当前用户凭据区。DaHe 数据库只保存固定凭据引用、配置版本和时间，不保存明文账号密码。浏览器 Worker 在需要登录时直接解析该固定引用，密码不得穿过主进程协议、命令行、日志、证据或诊断导出。程序不读取系统浏览器密码库，不使用默认 Edge 资料目录。

“业务连接”以成丰页面实际成功发出的“可结算、结算、按运单显示、重置、查询”请求为本次读取权威。在切换页面业务范围时，程序只放行完成这些状态切换所必需的精确只读列表请求；页面明确选中目标范围后才记录最终查询。最终查询的请求体和必要会话信息只在隔离 Worker 私有内存中短暂复用，并把列表、详情和磅单转换为系统已有的统一数据结构。请求值、会话头、原始响应和完整签名图片地址不得进入主进程协议、日志、数据库或证据。“验证连接”复用同一页面权威读取原语，并额外要求最终请求结构与冻结合同完全一致；两种连接不能在任务运行中静默切换。

成丰可结算运单数量是实时业务状态，不是配置、门槛或固定测试常量。程序每次查询都以本次页面成功响应声明的总数为准，完整分页并核对身份；同一次采集过程中总数或身份异常变化时停止并给出诊断，下一次独立采集可以得到不同数量。

运费结算和装卸车明细每次接管唯一成丰页后都必须禁用浏览器缓存、清除 HTTP 缓存并以 `ignoreCache=true` 重新加载固定业务页面，再建立本次页面权威查询。登录 Cookie 和页面存储继续保留。首次权威查询为零时，在同一标签页再执行一次完整的无缓存重新加载并重建查询条件；第二次仍为零才接受真实空结果。刷新或页面验证失败时不能使用刷新前的状态继续读取。

真实合同发现必须把登录与结构记录分成两个明确步骤。建立有效只读窗口后，“打开成丰登录页”只允许程序导航一次固定的成丰待结算入口；未登录时由成丰自行跳转到同域登录页。系统不接受人员输入的网址，也不提供通用导航。登录期间不记录网络结构；人员归还控制后，才允许按当前用途启动一次代码冻结的自动结构探针，或由人员只查看列表、详情和磅单。自动探针不得接受任意网址，只能放行精确只读请求并在发送前阻断未知请求和写请求。记录结果只保留域名、固定接口路径、字段名、字段类型和图片路径哈希，不保存登录信息、请求值、响应值、响应正文或签名图片地址。

“装卸车明细”按业务日创建独立只读 Job，从空闲会话直接进入 `/wayBill`，不得为了准备装卸车读取而访问或查询运费结算页。它先在唯一成丰页完成列表、详情和磅单图片读取并释放平台资源，再离线执行 OCR 结构化观测。候选下载范围默认从业务日 14:00 到任务启动时的服务器当前时间，也可在唯一一组报表参数中改为固定结束时间；每个任务冻结自己的实际起止时间。生产只读模式在该结构化结果上生成正式 Excel；只维护一组报表参数，不建设多模板管理。运费结算与装卸车明细复用可见窗口、单标签、无缓存刷新和只读审计基础管线，但使用各自精确的列表合同、ConflictKey、Job 和快照，不能在一条列表为空时静默切换到另一条列表冒充结果。

装卸车新读取默认从业务日当天 14:00 开始，到任务启动时的服务器系统当前时间结束；参数设置也可以为新任务指定业务日当天或次日的固定结束时间。每次任务冻结实际起止时间，成丰页面在该范围内返回并完成数量与身份对账的运单必须全部保留到本地观察层；采集发布前不得再按装货时间删减。旧版 13:30 起始和次日 14:30 安全尾部只用于历史证据重放，不能继续创建默认生产快照。本次精确查询的响应总数、分页数、唯一运单数和最终候选数必须相互一致。页面可见总数只有在证明确属同一精确查询范围时才参与对账；未筛选的全量页面总数不得与冻结范围强行比较。任何适用的数量或身份不一致都使本次获取视为不完整，不能用旧结果冒充成功。

装卸车正式查询不得先点击页面“重置”。系统以页面实际产生的只读查询为权威，只替换业务日、地点和分页等受控字段，保留页面或账号自带的安全业务范围。页面自带的筛选值只能停留在浏览器 Worker 私有内存中，不能进入日志、数据库、证据或诊断导出。

当前开发电脑以 `operational_read_only_active` 运行只读业务：机器判断正常的运单直接进入“可结算”，疑似放反、两票同角色、缺图、重量不一致、角色未知、识别分歧和技术故障仍按各自安全规则阻断。业务使用过程中持续保存机器证据和人工问题决定。历史试验、风险豁免和旧验收结论只通过 Git、ADR、Ledger 与既有证据保留，不再作为当前生产页面或现行开发阶段的前置流程。

生产只读模式只开放运费结算审核和装卸车明细报表。程序仍不确认结算、不付款、不撤销收货；旧程序保留为人工回退方案，但两套程序不能同时使用同一成丰账号自动采集。回退时保留新系统数据库、图片、报表和日志，不倒写旧数据库。

成功不只意味着 OCR 更快，还意味着正常任务不会被个别问题运单阻断，系统状态容易理解，失败可以恢复，历史数据可以长期查询，换一台 Windows 电脑也能通过明确的检测和配置完成部署。

## Brand Personality

清爽、克制、可靠。

界面应让财务人员感到平静、明确和可控。重要状态直接说明发生了什么、是否需要处理以及下一步是什么，不使用夸张语气或技术炫耀。

## Anti-references

- 不做传统 ERP 式的密集菜单、满屏边框和层层弹窗。
- 不做营销网站式的大幅动画、滚动特效和装饰性数据图。
- 不在财务操作页面暴露 CUDA、进程、DNS、JSON、置信度阈值等技术黑话。
- 不用颜色作为状态的唯一表达方式。
- 不让同一个按钮随后台状态变成无关操作；“暂停／继续”这组互逆动作可以按后端权威状态共用一个固定位置。
- 不把平台页面嵌入或改造成系统界面，不向成丰页面注入业务组件。
- 不用模糊的“登录失效”“处理失败”掩盖真实故障步骤。

## Design Principles

1. **业务动作先于技术实现**：页面围绕“开始审核、处理问题、生成明细、交给结算”等任务组织，技术设置退到独立系统区域。
2. **状态只有一个权威来源**：后端提供当前状态、可执行动作和不可执行原因，前端不重复猜测业务状态。
3. **证据与判断分开**：保留原始平台快照、OCR 提取、业务比较和人工决定，任何一步都可追溯。
4. **问题隔离，正常流程继续**：单张异常图片、问题运单或某项人工复核不能拖垮其余正常运单。
5. **渐进展示复杂度**：财务人员看到业务语言和必要选择；开发者在系统状态与诊断区域查看完整技术细节。
6. **不追踪使用人员**：系统依靠任务号、动作号、记录版本、时间和诊断编号追踪处理过程，不要求或保存姓名、工号、处理人、审核人和操作归属。
7. **一项工作只出现一次**：待核对、问题处理和结果查看都留在所属业务页面，不能通过重复首页、总待办或同义页面增加层级。
8. **本地判断不改写证据**：人工选择只追加正常或问题结论，不修改 OCR 原始值、平台重量、磅单图片或既有证据。
9. **诊断可复制但不泄密**：系统日志保留足够的阶段、进程和错误线索，同时以固定字段、脱敏和容量限制保护业务证据与本机环境。
10. **业务可用与历史验收分离**：现行生产能力只按当前合同和真实证据声明，不能改写历史失败、风险豁免或旧验证结论。
11. **简单优先**：只实现当前规模真正需要的能力；新增服务、运行时、框架或常驻基础设施必须先证明现有实现无法解决一个可测量的问题，并说明如何卸载和回退。

12. **业务日直接可用**：装卸车明细以业务日作为唯一查看范围。日期切换立即清空上一日的记录和数量，新的本地响应只有在日期仍与当前选择一致时才能显示；失败或乱序响应不得恢复旧日期结果。切换日期不通过任务历史寻找结果，也不触发重新采集。人员只修订出矿净重、出矿时间、收货净重和卸车时间；“保存”就是确认当前记录，所有异常字段一次提交，人工明确留空也能完成核对且不能被后续识别静默补回。
13. **获取流程一次完成**：运费结算和装卸车明细共用“业务范围、获取、任务控制、当前进度、业务结果”操作区。点击获取后优先后台登录；必须人工登录时自动打开独立窗口并检测完成，不再要求点击“登录完成”。财务页面不得显示连接器、阶段代码或内部任务名。
14. **生产响应优先**：默认降低采集并发并在显卡空闲后释放识别进程，使采集和识别期间本机仍能正常操作。性能设置只影响新任务，不能改变正在运行或历史任务。
15. **一屏只表达一次**：同一可视屏幕不得重复显示相同页面标题、阶段、进度、状态或操作。左侧导航已经明确页面时，内容区域不再重复页面标题；业务进度只保留一个后端权威投影。
16. **本地退出可恢复**：操作台提供明确的“退出程序”。退出停止领取新工作，保存已提交数据和最近检查点，只关闭当前 DaHe 实例登记的浏览器、OCR 和服务进程；再次启动从安全边界恢复。
17. **本地验证后增量复用**：重复获取仍以本次平台列表、详情、平台重量和已下载的磅单正文为准。本次图片全部落盘并释放平台后，才用当前 SHA-256 与本地索引判断能否复用 OCR 和比较结果；不得只凭运单号、详情版本、ETag、Last-Modified、签名地址或本地已有文件跳过本次图片正文读取。
18. **平台优先、识别离线**：一次获取先完整冻结列表、详情、平台重量和本次全部磅单图片，并在全部数量、身份和哈希对账通过后一次性发布在线结果。旧任务的 15/20/50/100 条网络批次只用于只读恢复兼容，新任务不再按业务批次等待或提交。全部平台证据完成后立即清除本次查询能力并释放成丰资源租约；唯一成丰页和 DaHe 专用 Profile 继续保留。之后由一个本地审核任务处理本次全部运单。单 GPU Worker 按冻结顺序连续处理同车装货、卸货两侧并在整车判断落盘后增加一次进度；完成一车后恢复跨任务公平轮转，不启动竞争显存的第二个 GPU Worker。在线阶段不得进行 OCR 或用历史候选跳过本次图片读取，平台占用时间和离线处理时间分别计量。
19. **结算只做交接**：运费结算可复制当前全部可结算运单号，或在 DaHe 自己的可见成丰窗口中打开固定“按运单显示”的批量筛选并填入这些运单号。程序必须核对平台最终筛选数量和身份集合后才能提示成功；已不在可结算范围的运单单独提示。程序只负责筛选并停留在结果页，结算确认、付款、撤销收货和后续平台操作始终由人员完成。
20. **报表直接生成**：装卸车报表只纳入有效出矿时间位于 `[业务日 14:00, 次日 14:00)` 的记录，候选下载范围不能替代该严格窗口。人工修订或磅单 OCR 时间优先；图片时间为空时才用平台装货时间判断纳入和排序，且 Excel 的出矿时间仍留空。两种时间都为空的记录不进入报表并继续保留在人工核对数据中。接口返回候选、纳入、窗口外排除和缺少有效时间排除数量。同一业务日再次生成时在完整校验后原子覆盖；文件被 Excel 占用时保留原文件并明确提示，历史版本通过数据库中的生成记录和文件哈希追踪，不再增加待确认步骤。
21. **时间和进度可理解**：业务任务持续显示已用时间；样本足够时显示基于本任务当前阶段的预计剩余时间，样本不足时只显示“正在估算”。刷新页面后必须延续后端时间基准，不能从零重新计时。成功、失败、取消和真实零条结果都必须保存结束时间并永久停止计时，预计剩余时间同时归零。
22. **装卸车只展示可处理结果**：仍在下载、识别或整理的运单只体现在顶部进度，不提前进入业务列表。机器可靠完成或人工已经处理的记录进入“已核对”，确实缺少业务字段的记录进入“待核对”；人工保存或明确留空全部异常字段后，记录立即转入“已核对”。
23. **连接状态后端权威**：业务导航与底部工具之间只显示一个“成丰平台连接状态”胶囊。它由后端根据 Worker、可见浏览器、登录和当前业务阶段投影为“浏览器关闭、正在打开、等待登录、连接就绪、正在读取、正在下载、连接异常”，前端不得自行猜测。

## Accessibility & Inclusion

- 以 WCAG 2.2 AA 作为可读性和交互目标。
- 支持 Windows 100%、125%、150% 和 200% 显示缩放；高缩放时允许布局重排，但不能丢失字段和关键操作。
- 主要操作可以使用键盘完成，并始终显示清晰的焦点状态。
- 状态同时使用文字、图标和颜色表达。
- 尊重系统“减少动画”设置；频繁业务操作不依赖动画反馈。
- 表格、表单和提示使用清楚、直接的中文，不要求用户理解技术术语。
- 普通正文对比度至少 4.5:1；大文字、图形对象和焦点指示至少 3:1。
