# 本地操作台设计合同

## 信息结构

操作台面向熟悉煤业物流但不熟悉技术的财务人员。一级导航只包含：

1. 业务
   - 运费结算
   - 装卸车明细
   - 派单
2. 历史数据
3. 系统

“运单审核”是运费结算中的处理阶段，不显示为独立页面。“今日工作”“待处理”“人工复核”不建立独立入口；待办、判断和结果都在对应业务页面中完成。

首次打开进入“运费结算”。应用只记住最后访问的一级页面，不保存图片、运单详情或表单内容到浏览器持久存储。

## 应用壳层

桌面端使用统一应用壳层。两个业务页面共用同一个主内容滚动容器、最大内容宽度、左右内边距、操作栏、进度栏、筛选栏和明细列表；业务页面内部不得再建立 `100vh` 或嵌套滚动。系统页面使用一级导航、二级导航和内容区：

- 左栏是稳定的一级导航。
- 中栏是当前模块的批次、筛选和项目列表。
- 右栏是所选项目的证据、业务结果和当前允许的操作。

左栏和中栏由轻量分隔线区分，不使用层层卡片。右栏拥有主要阅读空间。系统页面沿用同一结构，中栏改为“运行状态、运行诊断、识别模板、参数设置、数据管理”二级导航。一级或二级导航已经说明当前页面时，内容区只保留供辅助技术读取的隐藏标题，不再重复显示同名可见标题。

“系统”导航项旁保留一个透明背景、无边框的电源按钮，提示为“退出程序”。按钮保留 44×44px 点击区，图标严格居中，悬浮、焦点和按下时变红。确认退出后页面只显示“程序已退出，可以关闭此页面”，不把服务断开显示成重复错误。

## 页面职责

### 运费结算

页面只读取最新一次 `settlement_capture` 的业务投影。内部为了批量下载和 OCR 产生的一个或多个 Job 不显示为“审核批次”，页面也不提供“开始审核”。历史获取结果只在“历史数据”中查询。

页面从上到下只有三层：统一业务操作与进度、带数量的“全部、待核对、问题运单、可结算”筛选、完整展开的运单记录。操作栏使用“启动”、一个按后端动作切换的“暂停／继续”和独立“取消”；进度区只有一行阶段文字和一条清晰的 8px 轨道。状态、阶段、数量和进度只出现一次；不建立第二个状态标签、说明行或进度条。最新获取为零条时显示真实空状态；获取中途失败时保留本次已经原子提交的数据并明确显示“本次获取未完整”，不得回退显示上一次完整结果。

每条运单不显示表头、角色、单独“车牌”标签或重复结果文案。运单号和车牌先占一整行，下面横向显示较大的装货磅单与两组净重、卸货磅单与两组净重，以及“确认无误”“异常”两个直接决定。所有运单均完整展开，不再通过主从详情隐藏其他记录。

图片、“识别”和“平台”值必须处于同一个方向分组内，净重值下方不画装饰性分隔线。缩略图保持完整比例并按可视区域延迟加载，点击沿用统一大图查看器。所有待核对原因统一只标红识别净重，不标红图片边框或平台净重；能定位到装货或卸货侧时只标红该侧，净重缺失时标红“未识别”，无法可靠定位、两票配对冲突或历史原因不完整时同时标红两侧识别净重。异常不再叠加同义问题标签。`normal_ready` 选中“确认无误”，`confirmed_problem` 选中“异常”，待核对时均不预选。

两个决定不弹窗，不要求原因、正确值、备注或人员身份。页面不提供净重输入框、编辑图标、保存更正、自动补小数点、自动交换磅单、撤销收货或结算按钮。技术故障不形成运单人工项，只在唯一进度区和运行诊断中显示。

“可结算”筛选旁提供“复制运单号”和“打开成丰筛选”。前者按稳定顺序复制当前全部可结算运单号；后者只打开 DaHe 自己的可见成丰窗口，进入“按运单显示”，使用官方批量搜索填写并提交筛选条件，然后把筛选结果页留给人员。程序不点击结算、付款、撤销收货或任何业务写按钮。平台读取任务运行中不得建立该交接，避免把不同快照混在一起。

### 装卸车明细

页面使用与运费结算相同的业务操作、进度和筛选组件。点击“启动”后创建独立后台 Job，先在唯一成丰页完成平台列表、详情和图片读取，再清除本次控制、释放资源租约并离线识别；成丰页保持打开。业务日位于操作栏最右侧，可见文本和日历全部使用中国大陆中文年月日格式，离开页面再返回时保留最后选择的业务日和本地结果；底层接口仍使用 `YYYY-MM-DD`。只有已经完成机器物化的记录进入列表，筛选固定为“全部、已核对、待核对”；正在下载、识别或整理的记录只显示在顶部进度中。

装卸车新读取使用 14:00 起始的业务范围，结束端可以包含到次日 14:30 的候选安全尾部。页面不展示这些技术时间参数，但必须区分“完整获取”“本次获取未完整”；本次精确查询的响应总数、分页数、唯一运单数或最终候选数不一致时，不得显示完成状态或回退到旧结果。页面可见总数只在证明确属同一精确查询范围时参与对账，未筛选的全量页面总数不得造成误报。成丰页面已对账的候选不得被本地时间规则再次删减。

出矿时间采用 `[2026] / [08] / [08] [时] : [分] : [秒]`，卸车时间采用 `[2026] / [08] / [08] [时] : [分]`。斜杠和冒号位于输入框外且不可编辑；月、日、时、分、秒的一位数字在离开字段、按 Enter、Tab 或分隔符时补零，明确的一位月份可立即补零并前进。范围固定为月 1–12、实际日历日、时 0–23、分秒 0–59，非法值标红并禁止保存。支持方向键、Tab、退格和整段粘贴；卸车秒数直接舍弃，不进位。日期按平台方向时间、同车可靠时间、快照日期和业务日的安全优先级预填；只有日期可靠时不得用 `00:00` 冒充未知时分。四个业务字段使用与运费结算净重相同的数字字号和字重，控件宽度按内容收紧，图片获得主要阅读空间；保存按钮在整条记录中垂直居中。人工处理全部异常字段并保存后，该记录从“待核对”移动到“已核对”。

生产只读模式在完成的业务日旁显示 Excel 绿色的“生成报表”。首次生成前在参数设置中确认发运煤矿、煤种、卸货地点、查询地点关键词和输出目录；业务页面不重复展示这些设置。生成后直接得到正式文件。同一业务日再次生成会在校验后原子覆盖；文件被 Excel 占用时保留原文件并提示关闭 Excel 后重试。

### 派单

当前只提供正式模块位置和简洁空状态，不显示假数据或无效果按钮。

### 历史数据

中栏提供运单号、时间和业务结果查询以及结果列表；右栏使用时间线展示平台快照、OCR、机器判断、人工决定和失效关系。旧版人工更正显示为“历史更正（旧版）”，只读且不可继续编辑。

### 系统

- 运行状态：任务、资源和本地组件状态。
- 运行诊断：最近错误、诊断编号、脱敏诊断导出和实时后台日志。标题区提供“导出诊断包”“打开诊断目录”“复制诊断摘要”，导出只生成本地脱敏 ZIP，不自动发送。
- 识别模板：复用模板工作台。
- 参数设置：提供成丰账号的本地安全保存、更换和删除；不回显密码，不显示凭据存储细节。
- 参数设置：生产只读模式同时提供唯一一组装卸车报表参数；未确认时直接显示“保存并确认”，确认后可修改，不能建立多套模板层级。
- 数据管理：当前没有可安全使用的能力时只显示空状态。
- 锁定集和开发验证工具只出现在系统的开发区域。
- 运行状态只读显示业务连接状态，不重复放置“获取运单”“再次读取”或“关闭成丰”等业务按钮。
- “验证连接”及其合同、锁定集和影子验收操作保留在开发区域。连接方式只能在没有运行任务且浏览器会话已关闭时切换。

生产操作台不显示验证连接、锁定集复核、调度演练或其他开发入口。正式只读生产不显示首批保护计数；机器判断正常的运单直接进入“可结算”，需要人员处理的业务异常仍留在“待核对”或“问题运单”。页面不显示 Gate、fingerprint、连接器名称或验收器等技术术语。

技术故障不得进入财务待核对列表。财务页面只显示诊断编号；OCR 运行时、资源租约、数据库和网络细节只在系统诊断中显示。

运行诊断下方默认展开终端式日志区域。它使用深色中性表面和等宽字体显示时间、级别、来源和脱敏消息，提供筛选、搜索、暂停、自动跟随、复制可见日志和导出全部日志。用户向上滚动后停止自动跟随并显示新日志数量，不能强制把阅读位置拉回底部。

应用壳层左下角在“系统”右侧、退出图标左侧放置单图标更新按钮。其点击区、无边框样式、Tooltip、键盘聚焦和缩放行为与退出按钮一致。有新版时显示小型提示点；点击后显示当前版本、新版本和人工确认，不默认安装。任务运行、暂停或待恢复时安装操作必须禁用并说明原因。

### 成丰业务连接

公司电脑默认使用“业务连接”。运费结算和装卸车明细页面分别提供自己的获取按钮，点击就授权该次只读 Job；不建立工作日会话，不设置重复确认。页面只显示当前业务、采集阶段、进度、结果和具体失败原因，不展示请求参数、会话头或浏览器自动化术语。

DaHe 启动后异步打开一个使用专属 Profile 的可见 Edge 窗口。空闲窗口属于人员，程序不导航、不记录人工操作。点击启动后复用唯一成丰标签页，运费结算进入 `/billablewaybill`，装卸车明细进入 `/wayBill`；窗口最小化时恢复显示并选中该标签页。正常流程固定为“点击启动 → 显示当前业务页面并检查登录或自动登录一次 → 在同一页安装本次只读控制 → 禁用并清除缓存后以 `ignoreCache=true` 重新加载 → 冻结列表并分批读取详情与图片 → 核对范围完整性 → 清除本次请求能力并释放浏览器资源租约 → 离线 GPU OCR → 整理结果”。首次权威查询为零时只在同一标签页再完整刷新和查询一次。可见页面、浏览器进程、Profile 和登录状态继续保留；已有多个成丰标签时只保留一个，不关闭其他网站。网络批次只作为原子提交和恢复边界，不触发对旧批次的重复 OCR 编排；单条进度通过 SSE 瞬时推送。需要人工登录时最多保留一个窗口，刷新、关闭和重新打开操作台不得产生登录副作用。

每次查询都必须显示本次平台总数、已取得数量和遗漏状态；真实为零、查询失败和筛选后为空不能使用同一个空状态。正常完成后自动选中本次任务；登录失效时保留最近已提交批次，不把技术故障放入财务待核对列表。

业务连接失败时不得自动切换到其他范围或用旧快照冒充本次结果。本次精确查询的响应总数、分页和唯一身份数不一致时，页面持续显示“本次获取未完整”，并提供诊断编号；同范围页面显示总数存在时也必须一致。

## 视觉系统

界面采用受控的黑、白、灰、蓝体系：

- 黑、白、灰承担至少 90% 的页面面积。
- 蓝色只用于当前选中、链接、焦点和主要动作。
- 红色只用于错误、问题状态和业务异常。
- 不使用大面积蓝色背景、装饰性渐变、玻璃效果或指标卡堆叠。

默认字体为 `"Segoe UI", "Microsoft YaHei UI", system-ui, sans-serif`。全站只使用这一套字体。数字启用等宽数字特性。标题使用固定字号，不使用营销式超大标题，也不在标题后追加重复说明的小字。

应用名称固定分成“大禾物流”和“自动化平台”两行，字号高于其他标题。主导航和浏览器图标使用用户提供的透明背景大禾物流图形标识；导航中的标识不再包含图片自带文字。不同含义的独特操作不得复用同一图标；跨业务含义相同的“启动”等通用动作可以复用。

间距采用 4px 基础尺度：4、8、12、16、24、32、48px。相关字段紧密组合，不同业务分组通过更大留白分开。常用圆角为 8至10px；胶囊形只用于短状态标签。阴影只用于确实浮在页面上方的菜单或对话框。

按钮最小高度为 44px，宽度随图标和文字内容变化。一个区域只保留一个蓝色主要动作；危险操作使用红色语义但不依靠颜色单独表达。图标统一使用同一套线性图标，不混用字符、表情或不同图标库。

## 交互与状态

所有交互组件必须具备默认、悬停、焦点、按下、禁用、加载和错误状态。按钮文案说明结果，例如“确认无误”“异常”“导出诊断”，不使用“确定”“处理”等模糊词。

按钮用途、禁用原因和辅助技术解释使用统一提示组件。提示必须由鼠标悬浮和键盘聚焦打开，使用页面顶层定位避免被滚动容器裁切，并支持 Escape 关闭。禁用按钮的包装触发器仍可聚焦。性能预设和高级参数的提示必须用业务语言说明用途、速度和资源影响以及推荐值；每批保存数量应将实测默认 50 明确标为推荐。错误、警告、当前状态、字段名、确认后果、模板绘制步骤和空状态不能只存在于提示中。

点击操作的短暂反馈使用页面顶层居中的 Toast，不占用原有布局：成功为绿色、警告为黄色、失败为红色、普通信息为蓝灰色。Toast 显示五秒，最后 600ms 淡出，支持读屏；任务故障仍长期保留在业务进度和运行诊断中。

列表选择、后台刷新和保存人工决定不能让筛选、滚动位置或焦点无提示跳动。保存决定后从当前筛选中移除该项，并选择下一条待核对项目；没有下一项时显示明确空状态。

主要功能可使用键盘完成，焦点顺序与视觉顺序一致。点击目标不小于 44×44px。状态同时使用文字、图标和颜色。所有动画控制在 150至220ms，并在 `prefers-reduced-motion` 下关闭非必要过渡。

## 缩放与适配

- Windows 100% 和 125%：保持完整三栏。
- Windows 150%：左栏可压缩，中栏可以收起，但导航仍保留图标和中文名称。
- Windows 200%：切换为主从单页流程，先显示列表，选择后进入详情，并提供明确返回列表操作。
- 任何缩放下都不得隐藏平台重量、磅单重量、业务结果或人工判断按钮。
- 磅单图片保持完整比例，可放大查看，不得为了维持三栏而压缩到无法阅读。
- 宽度不低于 1280px 且高度不低于 720px 的详情首屏必须同时显示两张磅单、对应净重和人工判断按钮；更窄或高缩放布局允许恢复纵向滚动。

重点验收视口为 1366×768、1920×1080 和 2560×1440，并分别覆盖 Windows 100%、125%、150% 和 200% 缩放。1366px 视口业务内容左右内边距为 16px，1920px 和 2560px 为 24px；2K 屏幕限制内容与图片最大宽度，用于增加可见记录数量，而不是无意义放大。

## 参考图

## 装卸车精修工作区

装卸车明细采用按业务日读取的横向记录列表。顶部只有启动、一个暂停／继续、取消、生成报表、唯一进度和右侧业务日；日期变化后自动加载本地已保存记录。页面不显示任务选择器、历史任务列表、外层表格框、纵向格线或重复状态列。每条记录仅用一条浅灰横线分隔，并按运单与车牌、装货图片与字段、卸货图片与字段、保存动作四个区域对齐。

逐车可编辑字段只包括出矿净重、出矿时间、收货净重和卸车时间。所有输入框在静止状态保留清晰边框，异常只标红具体字段。保存按钮在没有有效修改时禁用。磅单缩略图完整显示并延迟加载；点击后使用覆盖层查看大图，工具栏仅包含左转、右转、放大、缩小、适合窗口和关闭。大图工具是唯一允许无边框的按钮组，但仍保留 44px 点击区域、Tooltip 和键盘焦点。

运费结算与装卸车共用紧凑操作栏和单阶段进度条。正常流程不显示第二行登录按钮或堆叠提示框；必须人工登录时自动弹出窗口，登录成功后自动继续。长错误完整换行显示，不能用省略号隐藏。

两个业务工作区必须复用 `BusinessOperationBar`、`BusinessProgress` 和 `BusinessFilterTabs` 的同一视觉与交互合同。1366×768 至 2560×1440 的 100% 和 125% 缩放保持操作密度；150% 和 200% 缩放只允许结构重排或使用一个“更多”菜单，不能复制第二套按钮。2K 屏幕限制正文和图片最大宽度，增加可见记录数量而不是等比例放大所有元素。

系统二级导航桌面宽度约 220px，1366px 下约 200px，使用与一级导航一致的无边框条目、轻量悬浮背景和蓝色选中状态。参数设置中的“成丰登录”“性能设置”“装卸车报表”使用相同区块标题；账号与密码同一行，已保存账号在账号框内遮挡显示，密码只显示不含假值的占位。高级数字设置使用标签旁 5–6 字符宽的紧凑输入框。左下角退出按钮的 Tooltip 通过 Portal 计算实际尺寸并限制在视口内，必须在 100%–200% 缩放和键盘聚焦时完整可见。

运行诊断在标题和导出操作之后立即显示固定高度的终端日志。“最近问题”等持续增长内容放在日志下方，默认折叠并按页读取，不得把日志推到页面底部。

以下图片是结构、密度、层级和视觉气质的参考，不是运行时资源，也不要求像素级复刻：

- `docs/design/references/freight-settlement-b-approved.png`（当前运费结算唯一结构参考）
- `docs/design/references/loading-unloading-details.png`
- `docs/design/references/loading-unloading-details-refined-a.png`（本次确认的精修 A 版）
- `docs/design/references/history.png`
- `docs/design/references/system-diagnostics.png`
- `docs/design/references/dispatch-placeholder.png`

参考图清单和 SHA-256 保存在 `docs/design/references/manifest.sha256`。只有最终不含净重编辑功能的运费结算图可以进入清单；此前包含修改净重表单的图片不得使用。
