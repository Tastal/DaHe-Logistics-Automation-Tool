# 大禾物流系统开发指南

## 1. 产品范围

大禾物流系统只服务本公司的一台 Windows 10/11 业务电脑和一台开发电脑。系统采用本地模块化单体，目标是稳定完成煤业物流中的重复读取、识别、核对、报表和历史数据沉淀。能用、易懂、可恢复和后续容易维护优先于技术形式完整。

当前生产状态为 `operational_read_only_active`，开放：

- 运费结算：从成丰读取待结算运单、保存两张磅单、OCR、角色与重量核对、本地问题隔离、复制运单号和打开成丰筛选。
- 装卸车明细：按业务日读取运单、结构化识别、人工补齐四个字段和生成固定 Excel。
- 历史数据、运行诊断、识别模板和参数设置。
- 派单只保留模块位置，尚未实施。

系统明确不做：

- 自动确认结算、付款、撤销收货或任何未批准的平台写操作。
- 人员账号、姓名、工号、处理人、审核人或操作归属。
- 自动交换疑似放反的装卸磅单。
- 用网页期望重量选择、排序或修正 OCR 候选。
- 与旧程序共享代码、数据库、端口、浏览器资料、凭据文件或运行进程。
- 为假设中的规模预建微服务、容器、云平台或第二套调度器。

结算和付款永久由人员在成丰平台完成。程序只能为这些人工操作准备本地核对结果和只读筛选页面。

## 2. 业务规则

### 2.1 运费结算

一次“启动”创建一个只读获取任务。业务页面只显示最新一次获取的统一结果，不显示内部下载批次、OCR Job、连接器名或技术阶段代码。一次获取必须形成明确的动态总数、唯一运单集合、两张磅单结果和最终业务结果；真实零条、部分完成和失败必须分开表达。

业务结果分为：

- `normal_ready`：两张票的角色、识别和平台重量满足当前规则，可进入人工平台结算准备。
- `awaiting_review`：存在业务证据不足或不一致，需要人员选择“确认无误”或“异常”。
- `confirmed_problem`：人员已将该运单列为问题运单，本地隔离，不进入可结算集合。
- 技术失败：只进入诊断，不得伪装成人工业务项。

人员不在本地修改磅单净重。人工操作只追加“确认无误”或“异常”决定，不填写正确值、原因、备注或人员信息，也不覆盖 OCR、平台重量和图片证据。证据变化后旧决定自动失效。

以下情况不得自动通过：

- 装卸票疑似放反、两张票同角色、重复图片、缺图、非磅单或角色未知。
- OCR 与平台净重不一致，或 CPU/GPU 的净重、单位、可靠性不一致。
- 吨单位的 OCR 原始净重为无小数点的四位及以上纯数字。
- 单位为公斤、单位未知或无法可靠换算。
- 程序无法证明关键证据完整。

四位整数吨值保留原文，不补小数点、不除以 100。公斤值暂不自动换算，统一进入人工核对。

“复制运单号”复制当前全部可结算运单号。“打开成丰筛选”只负责打开固定待结算页面、选择“按运单显示”、使用平台官方批量搜索、提交一次只读查询并核对返回身份。筛选完成后页面交给人员；程序不点击结算或其他写按钮。

### 2.2 装卸车明细

业务日是唯一查看范围。新任务从参数设置冻结候选下载范围，默认从业务日 14:00 到任务启动时的服务器当前时间；也可配置当天或次日的固定结束时间。当前时间不晚于开始时间时禁止启动。地点关键词使用参数设置中的正式值，当前为“榆林”。平台页面已完成数量和身份对账的候选必须全部保留到本地观察层，不得在采集发布前按报表时间二次过滤。列表只显示完成机器物化的记录：

- `reviewed`：机器可靠完成，或人工已经处理全部异常字段。
- `needs_review`：机器处理完成，但至少一个业务字段需要人员处理。
- `pending_processing`：仍在下载、识别或整理，只显示在顶部进度，不进入明细筛选。

页面筛选固定为“全部、已核对、待核对”。日期切换时先清空旧记录和计数，只有匹配当前业务日且属于最新请求代次的响应可以显示；加载失败不得回退旧日期。人工保存必须绑定当前业务日和记录版本，一次提交全部异常字段；明确留空以人工 `null` 修订保存。只有后端返回 `reviewed` 后记录才移入“已核对”并使用后端返回的最新计数。

逐车只允许修订：

- 出矿净重：吨，两位小数，可明确留空。
- 出矿时间：北京时间，保留秒。
- 收货净重：吨，两位小数，可明确留空。
- 卸车时间：北京时间，只保留到分钟，秒直接舍弃且不进位。

修订采用追加记录，不覆盖机器观察。对应磅单内容变化时相关修订失效；无关证据变化不能使修订失效。人工明确留空不能被后续 OCR 静默补回。

中文分段时间输入固定分为两行：年月日在第一行，时分秒或时分在第二行；键盘焦点顺序仍按年月日时分秒连续。范围固定为：月 1–12，日按实际月份和闰年，时 0–23，分和秒 0–59。一位数字在离开字段、按 Enter、Tab 或分隔符时补零；明确的一位月份可以立即补零并前进。非法值标红并禁止保存。

报表固定 10 列：序号、发运煤矿、计划日期、出矿时间、车牌号、出矿净重（吨）、收货净重（吨）、煤种、卸货地点、卸车时间。格式以锁定参考文件为准。报表统计窗口固定为 `[业务日 14:00, 次日 14:00)`，与候选下载范围分离。出矿时间按人工修订、装货磅单 OCR、平台装货时间的顺序取得；平台时间只用于图片时间缺失时的纳入判断和排序，Excel 出矿时间必须留空。两种时间都为空时排除但不删除人工核对数据。生成结果必须返回候选、纳入、窗口外排除和缺少有效时间排除数量。框线只覆盖标题、数据和汇总行 A:J；筛选只覆盖标题和数据；汇总行以下不允许样式。当前主体、当前业务日存在记录且待核对数量为零时即可直接生成，不再要求单独确认报表参数；全新安装第一次生成时在同一动作内物化内置默认参数。文件名为 `装卸车明细-<主体>-YYYY-MM-DD.xlsx`，同一主体同日重新生成在校验后原子覆盖；文件被 Excel 占用时保留原文件并提示关闭后重试。

### 2.3 派单

派单当前只保留导航位置和模块边界。未来实现可能涉及第三方 VPN、安卓应用和微信群信息识别；在业务流程、权限和安全合同确认前不得接入真实系统。

## 3. 当前架构

系统采用一个 FastAPI 主进程、一个 React 操作台、一个 SQLite 数据库、受控浏览器 Worker、一个持久 GPU OCR Worker 和独立 CPU OCR Worker。GPU 调度保持一个槽位，调度单位是冻结顺序中的一辆车。OCR v2 每次只接收同车一至两张图片，先按输入顺序完成一次共同快速批量推理，再只对未可靠命中的一侧执行旋转或增强；整车证据与业务判断一次落盘后进度增加一辆，随后恢复跨任务公平轮转。GPU 允许回退的技术故障必须废弃该车未提交的整组结果并由 CPU 重跑该车全部待识别图片，不能提交半套结果。前后端、调度、证据存储和业务决定只有一套权威实现。

依赖方向为：

```text
UI / Local API
→ application services and queries
→ domain contracts
→ ports
→ SQLite, filesystem, browser and OCR adapters
```

业务模块不得直接导入另一模块的内部表或实现；通过应用端口、事件或只读查询协作。主应用、CPU OCR、GPU OCR 和浏览器运行时分别锁定依赖，不把 CPU/GPU Paddle 安装在同一环境，不依赖系统全局 Python。

所有本地写接口使用幂等键和记录版本。后端提供状态和 `available_actions`，前端不得自行猜测按钮是否可用。

## 4. 任务、资源与恢复

核心对象：

- `Job`：一次用户业务任务。
- `WorkItem`：一条运单、图片或导出单元。
- `StageAttempt`：一个 WorkItem 在一个阶段的一次尝试。
- `Checkpoint`：已持久化、可安全恢复的边界。
- `ResourceSlot` 和 `Lease`：浏览器、GPU、CPU 和短数据库事务的容量与租约。
- `ConflictKey`：防止相同业务范围重复创建。

执行状态与业务结果必须分开。状态包括创建、排队、运行、等待资源、等待人员、等待外部、重试、暂停申请、已暂停、取消申请、已取消、成功和失败；阶段使用 `settlement.*`、`daily.*`、`audit.*` 等业务命名空间。

运费结算和装卸车可以并行存在，但成丰读取共享一个浏览器资源槽。OCR 阶段可以与另一业务的浏览器读取重叠。不得使用“系统中已有任务”作为全局锁。

在线列表、详情和图片阶段不支持暂停，只支持取消；浏览器读取需要在两秒内收到中止确认，否则只终止当前 DaHe 登记的 Worker。在线采集只有全部校验通过后才一次性发布，失败或取消丢弃整个未发布暂存结果。离线审核支持暂停和继续，并保留已经完成的单项结果。取消不是技术失败，不影响其他 Job。

人员从操作台正常退出时，当前运费结算和装卸车读取必须在本实例调度器与 Worker 停止后落为“已取消”，写入固定完成时间，重启后不自动续跑且计时不再增长。程序异常中断遗留的这两类读取在下次启动时落为技术中断，释放浏览器持有权，不伪装为仍在运行或自动恢复。其他离线任务继续使用各自已确认的恢复规则。

新业务 Job 使用 `whole_run_v1`：平台分页只枚举完整身份集合，不构成业务批次；详情并发最多 4，图片并发最多 6，完成后一次性提交一个在线结果并创建一个离线审核 Job。历史 `batch_v1` 的 15/20/50/100 条批次保持只读恢复兼容。单条进度事件只用于实时显示，不作为数据库检查点，因此不得为实时进度增加逐条事务。

## 5. 成丰只读连接

### 5.1 安全边界

所有程序请求必须限制在已批准的成丰 host、path、method、字段集合和只读业务。未知请求、重定向和所有平台写请求在发送前拒绝。密码只保存在 Windows Credential Manager；Cookie、令牌、请求值、签名地址和原始响应只允许短暂存在于浏览器 Worker 私有内存，不进入命令行、NDJSON、数据库、日志、证据或诊断导出。

业务人员点击“启动”即授权本次只读采集，不再重复询问旧程序状态。用户对两个已批准业务的真实成丰只读验收持续授权，开发验收无需每轮重复申请；该授权不包含结算、付款、撤销收货或任何其他平台写操作。旧程序和新程序仍不得同时使用同一账号自动采集；发生外部登录干扰时，本次任务应安全失败或重新登录一次，不能循环弹窗。

### 5.2 可见浏览器与人工接管

DaHe 启动后异步维护一个可见的独立 Edge 窗口，使用 DaHe 专属 Profile。操作台启动不能被浏览器启动、网络或登录阻塞。

空闲时窗口归人员使用，程序不导航、不记录、不触发人工结算行为。启动只读任务后，程序只复用一个成丰标签页并导航到当前业务固定入口：运费结算为 `/billablewaybill`，装卸车明细为 `/wayBill`；最小化窗口恢复为正常显示并选中该标签页。程序在该页安装新的控制令牌；自动读取期间人员不得操作该页。完成、真实零条、失败或取消后清除本次请求能力、释放浏览器租约并保留当前业务页面。检测到多个同域成丰页时只保留一个；其他网站页面不得关闭。

下次读取复用同一成丰页，但不能沿用人员操作过的页面状态。登录有效时直接读取；被挤掉、出现登录页或 401/403 时使用保存凭据自动登录一次；自动登录失败或验证码出现时，最多保留一个可见登录窗口。只有 DaHe 退出、人员重置连接、Profile 损坏或浏览器崩溃时关闭该窗口。

两个业务每次接管唯一成丰页后都必须通过 CDP 禁用缓存、清除 HTTP 缓存并以 `ignoreCache=true` 重新加载固定业务页面，保留 Cookie 和页面存储。刷新完成后重新验证登录、同源固定路径和页面控件，再建立本次权威查询；不得使用刷新前的查询状态。首次权威查询为零时，在同一标签页重复一次完整刷新和查询，第二次仍为零才接受。装卸车重建查询时仍不得点击“重置”，只替换业务日、地点和分页字段。刷新、可见窗口或固定页面验证失败必须在平台查询前安全失败。

运费结算和装卸车明细共用可见 Edge、单一成丰标签、窗口置前、登录检查、无缓存刷新、新响应等待和只读请求审计的基础管线。装卸车从空闲会话直接进入 `/wayBill`，不得先准备、访问或查询 `/billablewaybill`；两项业务的列表合同、查询字段、ConflictKey、快照和完整性校验仍各自独立。

浏览器进程生命周期与资源租约分离。保留可见窗口不等于占用 `platform_browser`，空闲窗口不得产生自动平台请求。

每次两个业务开始前必须同时核对持久化控制状态和 DaHe 实际登记的浏览器 Worker；不得仅因数据库显示 `ready` 就假定 Edge 仍在运行。持久化状态为就绪但 Worker 不存在时，重建本项目的可见运行时；终态 Job 遗留的自动持有权在启动时精确归还。新 Worker 仍必须经过单标签、可见窗口、固定路由和无缓存查询验证。

### 5.3 平台优先、识别离线

一次读取固定为：

```text
读取并冻结列表
→ 获取全部详情、平台重量和两张磅单
→ 核对平台总数、响应总数和唯一身份
→ 原子提交本次平台证据、清除页面控制并释放浏览器资源
→ 在本地用图片哈希核对历史结果
→ GPU OCR 新图片，必要时 CPU 降级
→ 比较并整理结果
```

在线阶段不得运行 OCR，也不得用旧缓存跳过本次平台图片正文读取。复用判断只能在本次图片落盘后使用当前 SHA-256、平台快照和 OCR/模板/规则指纹。平台重量变化只重新比较；图片变化重新 OCR 并使相关人工决定失效；OCR 或规则变化只失效对应层。

浏览器 Worker按页、详情和图片发送瞬时进度，操作台通过 SSE 显示单条变化；在线证据只在整轮数量、身份、文件和哈希校验通过后提交。离线审核按照冻结运单顺序逐车完成和发布，不得把一侧图片完成当成一辆车完成，也不得让后车越过前车插入页面。

### 5.4 装卸车范围完整性

装卸车查询固定使用 `/wayBill`、正式地点关键词和选定业务日。新读取默认从业务日当天 14:00 到任务启动时的服务器系统当前时间；参数设置也可选择业务日当天或次日的固定结束时间。每个任务必须冻结实际起止时间，当前时间不晚于开始时间时拒绝启动。旧版 13:30 起始和次日 14:30 安全尾部只允许重放已经封存的历史证据，不得继续作为新任务默认范围。程序不得在正式查询前点击页面“重置”；“重置”会把页面或账号自带的业务范围换成更窄的默认值。程序让页面产生最终权威查询，只替换经过验证的业务日、地点、冻结时间范围和分页字段；其他有界且不敏感的页面自带筛选值只允许保留在浏览器 Worker 私有内存中，不进入协议、日志、数据库或证据。

本次精确查询的响应总数、分页和唯一身份完成对账后，该身份集合就是本次读取的权威候选。页面显示总数只有在浏览器 Worker 能证明它属于同一查询范围时才参与对账；未筛选全量页面的显示总数属于不可比较证据，不得与精确查询总数强行对账。应用层必须原样冻结全部候选，不得再按装货时间、票据时间或本地推测范围做第二次过滤。最终候选数与权威唯一身份数不同必须以 `scope_complete=false` 停止，不能保存成成功快照。

每个快照保存脱敏的：可比较的平台页面显示总数（没有同范围证明时为空）、响应总数、响应分页数、实际读取分页数、唯一身份总数、最终候选数、范围哈希、完整性结论和诊断编号。请求体和响应正文不持久化。

只有响应总数、唯一身份和最终候选数量一致，分页完整且身份无重复时 `scope_complete=true`；存在同范围页面显示总数时，该总数也必须一致。不可比较的全量页面总数不得独自使精确查询失败。不一致时任务显示“本次获取未完整”，不能回退到旧快照或旧缓存冒充成功。专项对账必须能够比较集合并报告缺失身份及其排除阶段。

## 6. OCR 与安全路由

主应用不直接加载 Paddle。OCR 子进程只接收内容哈希、票据角色和数据目录内相对路径，不接收网页期望重量、凭据或大文件正文。v2 批量命令最多包含同一运单的两张图片，每张图片独立绑定 SHA-256、角色和顺序，结果必须保持相同顺序；一张图片的旧 v1 命令继续只读兼容。运行时身份、模型、模板、规则和源码指纹必须写入结构化证据。

GPU 是主路径，设备由探测和 smoke 选择，不写死 GPU 编号、型号或开发机路径。GPU Worker 固定为一个。GPU 明确失败后才能在运单边界使用 CPU；CPU 和 GPU 不同时争抢资源。CPU 路径必须始终可用。

票据角色结果允许 `loading`、`unloading` 和 `unknown`，不得强制二选一。上传槽位不是角色真值。疑似放反、同角色、重复或未知必须安全阻断，不自动交换。

重量使用 `Decimal` 和明确单位。普通净重与工厂净重严格隔离；所有比较在归一化后进行，不使用浮点近似。原始 OCR、候选、可靠性、异常原因和最终使用值分开保存。

## 7. 数据底座

正式产品数据默认位于 `%LOCALAPPDATA%\DaHeLogisticsAutomationTool`，程序位于 `%LOCALAPPDATA%\Programs\DaHeLogisticsAutomationTool`，源码、运行时、产品数据和旧程序数据完全分开。`%LOCALAPPDATA%\DaHeLogistics\production` 是 0.8.x 数据源，只能在旧程序停止后通过 SQLite online backup 和受控文件复制一次性迁入新目录，不得原地升级或删除旧数据。SQLite 使用 WAL、外键和短事务；不得持有数据库提交锁等待浏览器或 OCR。

日常开发以当前 Git 源码提交为最新权威，项目命令只使用 `.venv\Scripts\python.exe`。已安装程序是冻结发布，仅在人员明确要求新打包时更新；调试不得直接改写安装目录。每次开发交付分别报告 source HEAD、当前绑定 8877 的进程命令行和 installed identity，三者不得混称为“最新程序”。

平台快照、图片、OCR 观察、审核决定和人工动作采用追加式版本。图片按 SHA-256 内容寻址，相同内容只保存一份并保留所有业务引用。所有业务结果必须能追溯到平台快照、图片哈希、运行时和规则版本。

成丰平台数据身份必须包含签约主体。内部代码固定为 `shanxi_guienbo` 和 `shanghai_jinyisheng`；平台任务通过独立绑定表取得不可变主体，快照、观察、机器修订、人工修订、幂等记录和复用索引以“主体 + 运单身份”为唯一范围。装卸车报表以“主体 + 业务日”隔离，同一范围只允许一个当前报表，但必须保留已经标为 stale 的历史报表。Schema 0041 以前的既有记录只迁移为 `shanxi_guienbo`，不得猜测或重新归类。主体选择是带记录版本的后端权威本地状态；空闲切换不控制成丰页面，业务启动才精确切换会话主体并在发布结果前再次确认。

结构化运单、人工决定、任务、诊断和报表记录长期保留。处理完成且超过七天的原图自动清理尚未实施；实施前必须先证明引用、`hold`、备份和恢复安全，删除后仍保留图片哈希、结构化结果和业务引用。

备份使用 SQLite 在线备份和内容清单。发布、迁移和重大设置变更前必须创建备份，并在独立临时目录恢复，验证数据库完整性、外键、关键数量和证据哈希。不得把生产恢复测试指向正式目录。

For a completed `whole_run_v1` daily source, the current workspace is scoped by
the source snapshot rather than by newly appended machine-revision rows. When a
current observation has the same field fingerprint as the latest append-only
revision, the projection inherits that revision and binds it to the current
observation. Identical fields must not create duplicate revisions, and revision
deduplication must never remove a current-source waybill from the workspace.

## 8. 本地操作台

操作台业务导航只保留“运费结算、装卸车明细、派单”，业务标题旁提供两个签约主体切换，底部工具固定为“系统设置、历史数据、版本更新、退出程序”。业务导航与底部工具之间显示一个后端权威的成丰平台连接状态。系统默认进入运行诊断，二级导航只保留运行诊断、识别模板和参数设置。详细页面结构、视觉、缩放、键盘、Toast、Tooltip、图标和中文时间控件以 `DESIGN.md` 为唯一合同，本指南不复制完整规格。

长期 UX 原则：

- 同一屏幕不重复标题、状态、阶段、进度、按钮或说明。
- 财务页面只使用业务语言，技术细节进入运行诊断。
- 运费结算和装卸车共用操作栏、进度、筛选和主滚动容器。
- 持续错误留在进度或诊断；短暂反馈使用页面中央 Toast。
- 所有互动控件静止状态可辨识，主要操作可键盘完成。
- 覆盖 1366×768、1920×1080、2560×1440 和 Windows 100%、125%、150%、200% 缩放。

## 9. 测试、发布与回退

权威检查命令只维护在 `README.md`。每次交付至少运行受影响模块的单元、合同、集成和浏览器测试；涉及 UI 时必须用真实 Edge 生成并逐张目视检查 1366×768、1920×1080、2560×1440 和 100%、125%、150%、200% 缩放证据。自动断言通过但截图未打开检查，UI Gate 仍然失败。不得用更新快照掩盖视觉错误。

发布门禁包括：

- 当前 Git 源码、构建、发布目录、已安装版本和指纹一致。
- 项目 `.venv`、CPU OCR、GPU OCR 和浏览器运行时 smoke 通过。
- 本地 HTTP 健康端点实际返回 200；端口冲突时明确停止，不杀未知进程、不自动换端口。
- 精确查询的响应总数、唯一身份和最终业务结果数量一致；同范围页面显示总数存在时也一致。
- 平台程序写请求、禁止请求和意外重定向为零。
- 暂停、立即取消、崩溃和重启不丢失或重复已提交结果。
- 日志、数据库、协议和诊断导出不泄露凭据或平台私有内容。
- 备份能在临时目录恢复。

正式安装使用 PyInstaller one-folder 业务程序和 NSIS 当前用户安装器，不使用 UPX、加壳、混淆或管理员权限。稳定入口为安装根目录下的 `DaHeLauncher.exe`，业务程序位于 `versions\<version>`，桌面只创建“大禾物流自动化平台”一个快捷方式。Launcher 嵌入 16–256px 多尺寸大禾 Logo，安装器和开发快捷方式都显式绑定该图标。操作台只在两行产品标题文字正下方显示 readiness 返回的低强调当前版本，前端不得硬编码或显示缓存版本。CPU OCR 必须始终可用；正式 GPU 附加包通过同版本更新器安装到独立覆盖层，只有现场资格、模型、Worker、CPU generation 和正式包身份全部一致才允许作为主运行时。GPU 安装或资格失效不得破坏 CPU composition，旧版本回滚必须能够忽略 GPU 覆盖层。

开发候选使用项目源码和 `.venv`，独立浏览器及 OCR 运行时统一位于 `%LOCALAPPDATA%\DaHeLogisticsDevelopment\runtimes`，但业务数据继续使用 `%LOCALAPPDATA%\DaHeLogisticsAutomationTool`。调试不得修改已安装目录。开发完成后必须先由用户验收源码候选；只有用户明确要求“发布源码和上传 Release”后，才允许从未变化的干净提交构建安装包、更新公共仓库或部署其他电脑。验收后源码若变化，必须重跑受影响检查、截图门和一次最终全量检查。

浏览器、CPU OCR 和 GPU OCR 正式运行时必须分别使用固定版本和 SHA-256 的 Python 官方 64 位嵌入式发行包，根目录 `python.exe` 是唯一正式解释器。构建时只从已验证的隔离环境复制锁定依赖并重新放入嵌入式布局，不发布虚拟环境的 `Scripts`、`pyvenv.cfg`、`direct_url.json`、缓存或字节码。浏览器正式运行时只带 Worker 及其锁定依赖，使用系统已安装的 Microsoft Edge；构建期 smoke store、浏览器 Profile、Cookie、历史记录和登录资料不得进入发布资产。正式构建发现开发机绝对路径、源码指纹不一致、依赖清单不一致或运行时无法独立启动时必须失败；开发环境仍可使用各自的虚拟环境。CPU/GPU 保持物理隔离。正式 CPU 使用 `runtimes\ocr-cpu` 的扁平 composition；正式 GPU 使用 `runtimes\g`、本机资格 `runtimes\gq\qualification.json` 和原子指针 `runtimes\active-gpu-addon.json`。状态查询和每次运行时选型必须重新核对显卡 UUID、驱动、显存和资格证据；不匹配时拒绝 GPU 并继续使用已验证 CPU。

正式 CPU OCR 包必须使用 ADR-071 的扁平 composition 布局，同时只读兼容既有 generation 布局。构建必须按 20 字符 Windows 用户名投影普通用户安装路径，并在文件超过 259 字符或目录超过 247 字符时失败；安装器必须使用短暂存目录、先检查磁盘与路径预算、完整校验后原子启用。产品不得为了安装成功而修改系统长路径策略、请求管理员权限或隐藏底层错误阶段。

独立 `DaHeUpdater.exe` 只从 `Tastal/DaHe-Logistics-Automation-Tool` 的 HTTPS 正式 Release 读取固定清单，不接入公共代理或另一在线发布源。应用 ZIP 下载使用绑定清单身份的持久化 partial 文件、HTTP Range、严格 Content-Range/大小/SHA-256 校验和有限退避重试；服务器忽略 Range 时从头安全重下，清单变化或哈希失败时删除旧 partial。也可以由人员本地导入正式 `update-manifest.json` 和应用 ZIP，但必须复用同一版本、防降级、Schema、文件名、大小和哈希门禁；已校验导入状态必须可跨主程序重启继续安装。它必须拒绝降级、异常文件名、非 GitHub 下载域、哈希或大小不匹配及 Schema 范围不支持的更新。网络失败不影响业务；检查可自动，每次人工点击更新入口仍必须重新检查在线状态并清除过期候选，安装必须由用户确认，且只在无运行、暂停或待恢复任务时进行。新版不覆盖当前版本；当前指针仅在资源校验、数据库预检和正式迁移通过后原子切换。应用 ZIP 必须携带同版本更新器，主程序优先使用版本目录内更新器，稳定根目录更新器只保留为旧版本首次升级入口。

正式安装负载必须包含 `operational-contract-install.json` 所声明的全部只读平台合同，而不只包含标记文件和模板包。构建时逐项验证相对路径、大小和 SHA-256，首次启动时再从只读 seed 幂等复制并验证到正式数据根；缺失、越界、符号链接、内容冲突或数量不一致均必须在业务开放前失败。干净安装不得依赖开发数据根、历史升级残留或人工远程修补才获得业务读取资格。

每个 Release 清单必须声明最低 Schema、目标 Schema、Alembic revision 和最低更新器版本。更新前在数据库副本上完整迁移和预检；通过后才停止本项目进程、用 SQLite online backup 备份正式库并迁移。新版 readiness 未通过且业务界面尚未开放时，自动恢复备份并切回旧版；新版产生用户操作后不得自动覆盖数据回滚。每个 Release 至少验证空数据库、上一正式 Schema 和重复迁移。

正式诊断完全本地化：Breadcrumbs 只记录页面、动作类型、任务 ID、结果、时间和错误编号；Environment Snapshot 记录软件、Schema、SQLite 完整性、Windows、Edge/OCR 运行时、磁盘和 CPU/GPU 可用性，并权威报告 GPU 附加包状态、现场资格、当前主运行时、CPU 回退可用性、GPU 包版本和诊断编号。诊断 ZIP 最多最近七天、20 MB，仅保留新记录，不得包含数据库、图片、浏览器资料、凭据、平台原始数据或 OCR 原文，不自动上传。

发布只停止当前 DaHe 自身服务和登记的 Worker，不触碰旧程序、未知 Python、Edge、Chrome 或其他本地服务。只有 readiness 的 HTTP 200 同时返回指针声明的版本、提交、资源哈希和 Schema revision，才允许开放操作台。

回退时停止新任务、保留新系统数据库、图片、报表和日志、关闭 DaHe 自己的 Worker，然后由人员启动旧程序。不得删除新数据、倒写旧数据库或让两套程序同时自动连接成丰。

## 10. 后续阶段

### 当前业务稳定性收口

完成可见成丰窗口、单一成丰标签、装卸车范围完整性、单条实时进度、两秒内取消、时间输入和页面一致性；用真实业务日核对数量和身份。

### Windows 打包与开发电脑验收

完善隐藏启动、快捷方式、依赖 smoke、升级前备份、版本一致性和一键回退。在开发电脑连续完成运费结算、装卸车和报表业务。

### 公司电脑试运行

在公司 Windows 10/11 电脑安装独立发布，配置凭据和输出目录，验证中国大陆网络、GPU/CPU 自适应、权限、重启恢复和非技术人员操作。

### 生产数据整理与保留

增加历史数据整理、自动备份、备份恢复演练和处理完成图片的七天清理。结构化数据、哈希和业务决定持续保留。

### 运费结算交接优化

继续优化可结算运单复制、成丰批量筛选的速度和身份核对；仍不自动确认结算、付款或撤销收货。

### 派单开发

在 VPN、安卓应用、微信群信息和人工边界确认后单独设计业务合同、数据模型和验收流程，不复用结算模块内部实现。
